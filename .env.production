# Loaded automatically by Vite for `npm run build` (production).
# Live mode: the built app talks to the real Express API + PostgreSQL database
# (served from the same origin), instead of the in-browser mock.
VITE_MOCK=false
